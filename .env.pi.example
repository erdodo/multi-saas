# =============================================================
#  Raspberry Pi — Production (.env)
#  Kullanım: sudo docker compose up --build -d
#  Bu dosyayı Pi'ye manuel olarak oluştur, Git'e commit etme!
#  cp .env.pi.example .env   →  değerleri doldur
# =============================================================

# postgres → Docker Compose servis adı (localhost değil!)
DATABASE_URL="postgresql://postgresql:postgresql@postgres:5432/postgresql"

# openssl rand -base64 32  ile güvenli değer üret
NEXTAUTH_SECRET="jV0sMKY0MQP7Vzkf9p1Gi04Ueb3LCwaHC2L0zq1ofFk="

# Pi'nin dışa açık adresi
NEXTAUTH_URL="https://saas.erdoganyesil.org"

# Cloudflare Tunnel token'ı
# Cloudflare Zero Trust → Tunnels → Token kısmından al
CLOUDFLARE_TUNNEL_TOKEN="buraya_cloudflare_tunnel_tokenini_yaz"
